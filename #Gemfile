#Gemfile

source 'https://rubygems.org'

gem "sintra"
gem "activerecord"
gem "sinatra=activerecord"
gem 'sintra-flash'
gem 'sintra-redirect-with-flash'

group development do 
  gem 'sqlite3'
  gem "tux"
end

group :production do 
  gem 'pg'
end
